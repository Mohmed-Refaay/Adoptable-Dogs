query getDogByName($name: String!) {
  dog(name: $name) {
    name
    attributes {
      key
      value
    }
    description
    ageInWeeks
    image
    sex
    color
    breed
    fee
    weight
    availableDate
  }
}
